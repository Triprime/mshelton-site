<!DOCTYPE html>
<html>

  <head>
    <title>MsheltonSite</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    
    <%= render 'layouts/navigation_bar' %>

    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: name %>
    <% end %>

    <%= yield %>

  <footer>
    <p class="footer">&copy;<%= Time.now.year %>, Matt Shelton | <em><%= Rails.env %></em></p>
  </footer>

  </body>
</html>
