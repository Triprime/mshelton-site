<h2>Sign In</h2>
<%= simple_form_for :signin, url: create_session_path do |f| %>
  <%= f.input :email, input_html: {value: (params[:signin][:email] if params.keys.include?('signin'))} %>
  <%= f.input :password %>
<%= f.button :submit, "Log In", class: "btn btn-primary btn-lg" %>
<% end %>

